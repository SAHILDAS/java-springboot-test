spring:
  application:
    name: blogplatform

  datasource:
    url: jdbc:postgresql://localhost:5432/blog_platform
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

  flyway:
    enabled: false

server:
  port: 8081
